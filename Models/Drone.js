'use strict';
class Drone {
    constructor(totalCapacity, consumption) {
        this.totalCapacity = totalCapacity;
        this.consumption = consumption;
        this.capacity = totalCapacity;
    }
}
